# Leave REACT_APP_API_URL empty so all /api/* calls go through
# Vercel's proxy (defined in vercel.json), which routes to Render.
# This avoids CORS issues and cold-start problems with direct Render calls.
REACT_APP_API_URL=
